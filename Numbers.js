/*
NUMBERS
- can underscore when writing big numbers, e.g. let billion = 1_000_000_000
                let million = 1e6
- some methods : Math.floor() =>  3.1 => 3 ; -1.1 => -2 (rounds down)
                 Math.ceil() => 3.1 => 4 (rounds up)
                 Math.round() => nearest integer
                 Math.trunc() => removes decimals
                 Math.random() => (0 < x < 1)
                 Math.pow(2, 10) => 2 pow 10
                 12.36.toFixed(1) = 12.4
                 - some operations suffer from lack of precision
- isNaN() , isFinite()
- parseInt() , parseFloat()

parseInt('100px') / 100
parseFloat('12.3.4') / 12.3
parseInt('a123') / NaN

*/